# GPU Monitoring

## Prerequisits

- Docker
- Docker Compose
- NVIDIA GPU


## Usage

- Create Directories
    ```
	mkdir -p prometheus/data 
	mkdir -p prometheus/textfiles
	chmod -R 777 prometheus
    ```
- Start
    ```
	docker compose up -d
    ```


